<%= form_with model: [@group, @member] do |m| %>
  <%= m.label :nick_name %>
  <%= m.text_field :nick_name %>
  <%= m.hidden_field :group_id %>
  <%= m.submit "Add member", class: "btn btn-primary" %>
<% end %>
