<h1>Add members to <%= @group.name %> </h1>

<%= form_with model: [:group, @member] do |f| %>
  <%= f.label :nick_name %>
  <%= f.text_field :nick_name %>
  <%= f.hidden_field :group_id %>
  <%= f.submit "Add member", class: "btn btn-primary" %>
<% end %>
